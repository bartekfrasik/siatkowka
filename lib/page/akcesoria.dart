import 'package:flutter/material.dart';

class AkcesoriaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: const Text('Akcesoria Siatkarskie'),
          centerTitle: true,
          backgroundColor: Colors.green,
        ),
      );
}
